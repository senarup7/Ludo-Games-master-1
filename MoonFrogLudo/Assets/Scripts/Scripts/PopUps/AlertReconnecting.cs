using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlertReconnecting : MonoBehaviour {




    // Update is called once per frame
    void Update()
    {
        //send ping 
     /*   if (WSESocket.Inst.Connected)
        {
            Destroy(this.gameObject);

        }*/
    }
}
